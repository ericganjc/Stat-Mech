# 第 6 章开场提示（用于手工新建任务）

用途：第 5 章交付并收尾后，第 6 章在新任务里从零开始。手工新建任务后，把下面代码块里的整段内容粘贴过去即可。

````
继续《统计力学自学讲义》项目，工作目录 /Users/ericgan/PhD/Statistical-Mechanics。本轮任务：编写**第 6 章「量子统计力学（Quantum Statistical Mechanics）」**。

开场请先完整读一遍 `PROJECT_HANDOFF.md`——它记录了这个项目的全部硬性要求、LaTeX 宏与编号约定、环境事实、每章标准流程、当前进度、各章页码范围，以及历次用户拍板，是唯一权威交接文档。除此之外**只读 `text.pdf` 的第 6 章相关页面**，开篇请先用 text.pdf 逐页核对本章正文的起止页（已知第 6 章正文自 **p.156** 起，p.156 首行即章标题；旧页码表所记 161 有误），并核对习题页（应在「不译页码表」的 175–180 一带），核对后**修正 `PROJECT_HANDOFF.md` 里的页码表**。文件夹里的其它文档（`notes/`、`Statistical Physics of Particles (Mehran Kardar).pdf` 等）一律不要打开；`HANDOFF.md`、`LEARNINGS.md` 只是项目最早的通用规范参考，本轮无需重读。

本章要点复述（细节以 PROJECT_HANDOFF.md 为准）：

1. **严格照原文翻译，不加任何自己的思考、推导、注释、改写**；原文疑似笔误照译，另记入 `errata.tex`（该文件独立编译，用 `./build-errata.sh` 验证）。
2. 只译正文不译习题；公式、图、表编号与原文一一对应，按章编号（`\label{eq:6.N}` 等）。
3. 章、节标题中英双语，中文在前、英文在后。人名一律不译。
4. 图只放占位框 + 中文图注，用户自己截图；命名 `fig/fig6-<图号>.png|jpg|pdf`（文件存在时 `\cnfigure` 会自动替换占位框）；原文中不编号的插图用 `\fbox` 无编号占位。
5. 术语行内格式「中文（*English*）」用 `\term{中文}{English}{6.key}`，**三个参数必须齐全**；**同一术语全书只在首次出现处埋锚点**，第 1–5 章已锚定的词本章只保留意大利斜体英文（可查各章文件末尾的术语注释块，或 `grep -ho "term:[0-9.a-z-]*" chapters/*.aux | sort -u`）。术语的中文名必须用**规范的中文物理学术词汇**。用户已拍板：**凡原文中第一次出现的术语一律收入附录 B**；强调性斜体（`\emph{}`）不收录。另：原书**小节标题与行内小标题都是粗体、不是斜体**，标题里的词不属「原文中的斜体术语」，不收录、不埋锚点。
6. 章末追加「本章斜体术语清单」注释块，区分「附录 B 候选术语」与「强调性斜体（不收录）」，并列出已在前面各章锚定的重复出现词及其首次锚定章节。
7. 流程：`bash _extract/render.sh <起页> <止页>` 渲染原文 → 每 4–6 页读一次 `_extract/pages/*-small.jpg` 缩图 → 译入 `chapters/chap06.tex`（用 `% p.NN` 标记原页）→ `./build.sh` 检查溢出与失效引用 → 用几行脚本核对 `chapters/chap06.aux` 里的编号连续性（`\newlabel{eq:6.N}` 的编号必须等于 6.N 且不缺号，图号同理）。
8. **一章一交付**：本章编译通过后即停下来交给用户检查，报告编译结果、编号核对结果、术语清单和需要用户拍板的事项，等用户确认后再开第 7 章。

汇报要求（用户硬性规定）：

- 每轮结束时**分别汇报两项体量指标，不得合并叙述**：(a) **对话体积** = 本会话 rollout 文件的磁盘体积（MB）；(b) **token 用量** = 最近一次请求的 input token 数 / 上下文窗口上限（131072）。两者是不同东西，都要给出数字。

上下文卫生（重要）：本机用 DeepSeek 接口，单次请求体积有硬上限，历史图片是主要风险。只读缩图（`-small.jpg`），同一张图不重复读，单任务累积图片控制在个位数；命令输出只取片段（`head`/`tail`/`grep`），不要把大文件整份打进对话；结论写进工作区文件而不是留在对话里。

当前进度：第 1、2 章已验收；第 3、4、5 章正文已译完并编译通过（第 5 章刚按用户裁决完成修订：小节标题不收录、`critical isochore` 补锚点、第 1 章回补 van der Waals 相关三个锚点、附录 A 改为记量纲不记页码）。第 6、7 章仅有章标题占位。附录 A 只列符号、含义与**量纲**（不记页码）；附录 B 的页码由术语锚点自动取。
````
