# 经验错题本 · LEARNINGS（第四章 · 继承版）

> 本文档继承自 [chap3/LEARNINGS.md](../chap3/LEARNINGS.md)，保留可复用的跨章教训条目，已剔除第一至三章特有的具体问题、行号与公式推导细节（如 CG 适用条件、ℓ₂/ℓ₁ 共用稀疏变换、两章的梯度 ½ 约定差异等，需要时回查 chap3/LEARNINGS.md）。
> 每次新会话开始前应速览此文档；**开新章翻译前必须再读一遍**，逐条检查是否踩到已知的坑。
> 继承日期：2026-09-10；最后更新：2026-09-10（第四章开工前的继承版本）

---

## [跨章流程] 章末收尾与开新章的标准动作

**触发场景**：一章写完后转入下一章

**标准解法**：
1. **章末收尾**：把本章 `HANDOFF.md`/`LEARNINGS.md` 更新到最新版本（会话记录、当前文件状态、待决策项），并在**下一章目录新建继承版 HANDOFF/LEARNINGS**——只搬跨章内容（工作模式、行文风格、术语与符号约定、编译习惯、踩坑模式），本章特有的细节问题与具体公式**不继承**。
2. **开新章**：**先照英文原文翻译**成 LaTeX，**然后读一遍继承版 HANDOFF/LEARNINGS**，逐条检查是否踩到已知的坑（条件清单、符号约定、浮动体脚注、术语格式等），最后编译验证。
3. "记忆文档"在本项目中**专指各级目录下的 HANDOFF 与 LEARNINGS**，所有更新与继承只针对它们，不另建文件。

**指令前缀**："章末更新本章 HANDOFF/LEARNINGS + 下一章建继承文档；开新章先翻译、再读记忆文档查踩坑。"

**来源**：chap3 会话 S3（2026-09-10）

---

## [跨章约定] 正文不出现参考文献引用；图片由用户补

**触发场景**：撰写新章节

**标准解法**：
1. `\cite` 一律不用，正文不出现参考文献引用（`\printbibliography` 可保留但不产生内容；相应编译只需 `xelatex ×2`，无需 biber）
2. 图片：AI 只预留 `\includegraphics[width=...]{文件名}` 占位与中文图注，**用户自行补图**，不代为提取或制作

**指令前缀**："不留 \cite；图片留占位，用户自己放。"

**来源**：chap3 会话 S3（2026-09-10，用户为第四章定下的约定）

---

## [数学推导] 推导链中定义项不可中途丢失

**触发场景**：多步骤数学推导，中间步骤省略了前面定义的项

**标准解法**：若步骤 A 定义了包含 $T$ 项的表达式，则后续 B、C、D 步骤中对同一表达式的变换必须一致保留 $T$。要么全程保留，要么步骤 A 就声明省略并在结论处汇总补回。

**指令前缀**："推导中所有定义项必须贯穿始终。禁止前面定义了后面又丢了。"

---

## [数学推导] 优化推导默认使用正则化一般形式

**触发场景**：涉及逆问题/优化问题的数学推导

**标准解法**：所有代价函数从完整正则化形式出发：$\|\text{数据项}\|^2 + \lambda\mathcal{R}(\cdot)$。若后续为简化讨论而省略某几项，需显式声明"以下暂时略去 X 项，仅聚焦 Y"，并在恢复时显式补回。

**指令前缀**："从正则化一般形式出发推导，保留所有项。如需简化某步，显式声明省略内容及原因。"

---

## [数学推导] 脚注必须补全原文疑点而非绕开

**触发场景**：原文记号有歧义或不严谨，需要添加脚注说明

**标准解法**：凡脚注需三层结构：
1. 原文写了什么（直接引用原文记号）
2. 哪里有问题（精确指出矛盾或歧义）
3. 正确的理解是什么（严谨推导，完整一贯）

用户可能预留空占位（如 `\footnote{勘误：}`）或指定标签与开头——填入时**保留用户的标签与开头，只补内容**；多个勘误点合在一个脚注时用①②分条（用户习惯）。

**指令前缀**："注释时先指出原文记号问题，再给出正确推导。禁止跳过'问题定位'直接写正确版本。"

---

## [数学推导] 微分算子解释必须区分函数签名

**触发场景**：同时解释一阶导和二阶导算子（Jacobian/Hessian、梯度/黑塞等）

**标准解法**：对每个被作用的函数，明确说明：
- 函数签名（$\mathbb{R}^n \to \mathbb{R}^m$ 还是 $\mathbb{R}^n \to \mathbb{R}$）
- 算子作用后的结果形状（$m \times n$ 矩阵 / $n \times n$ 矩阵 / $m \times n \times n$ 张量）
- 是否可逆（方阵可逆 vs 张量不可求逆）

**指令前缀**："涉及微分算子时，先明确函数签名→算子结果形状→可逆性。三者缺一不可。"

---

## [数学推导] 矩阵值函数求导：先判值域类型，再谈张量与缩并

**触发场景**：对矩阵值函数 $\mathbf{E}(\bm{\theta})$ 使用 $\mathcal{D}$、$\mathcal{H}$ 记号

**标准解法**：先判定值域类型并检查字体：值域 $\mathbb{R}^{m\times n}$ 为矩阵值函数（加粗正体），值域 $\mathbb{R}^k$ 为向量值函数（加粗斜体）。Jacobian 严格定义于向量值函数；对矩阵值函数，$\mathcal{D}\mathbf{E}$ 是逐元素求导的三阶张量（形状 $m\times n\times p$），与 $d\bm{\theta}$ 缩并后才得到 $m\times n$ 矩阵；等价地可向量化后再取标准 Jacobian。需要 Hessian 时须先固定其余变量构造向量值前向映射，再落到标量代价函数上。

**指令前缀**："涉及矩阵值函数求导，先说明值域类型与字体，再说明 $\mathcal{D}\mathbf{E}$ 为三阶张量、缩并后为矩阵。"

---

## [数学推导] 显式解必须注明适用条件

**触发场景**：推导中给出了矩阵求逆的显式解

**标准解法**：任何形如 $\hat{\bm{x}} = \mathbf{A}^{-1}\bm{b}$ 的表达式，必须在上下文中注明 $\mathbf{A}$ 可逆的条件（列满秩 / 正定 / 全采样等）。

**指令前缀**："给出显式解时同时注明矩阵可逆的适用条件。"

---

## [数学推导] 推导前必查条件清单（可逆性/正定性/秩/采样情形）

**触发场景**：任何数学推导（收敛性证明、显式解、算法分析）——用户铁律："数学推导一定一定要注意条件！！！以后每一次推导都要先看一眼条件是什么！"

**标准解法**：动手前先写条件清单，缺一不可：
- 矩阵形状与秩（如 $\mathbf{E}\in\mathbb{C}^{m\times n}$，$m$ 与 $n$ 的关系）
- 可逆性/正定性：$\mathbf{E}^H\mathbf{E}$ 恒为 Hermitian 半正定；正定 $\Longleftrightarrow$ $\mathbf{E}$ 列满秩（$m\ge n$ 且零空间平凡）
- 采样情形：欠采样 ≠ 秩亏——并行成像下 $m=Tp$，加速倍率 $R=n/p\le T$ 时 $m\ge n$，线圈灵敏度多样性使 $\mathbf{E}$ 通常列满秩；$m<n$ 才真正秩亏
- 条件不满足时结论如何修正（伪逆 $\mathbf{E}^{\dagger}$、最小范数解、定理不适用），而不是静默沿用

**指令前缀**："每次推导先列条件（形状/秩/可逆性/正定性/采样情形），条件不满足时说明修正方式。"

**来源**：chap3 会话 S2（2026-09-06）

---

## [数学推导] 跨章一致性疑问：先读两边原文再下结论

**触发场景**：用户质疑两处内容矛盾（如两章同一概念写法不一致）

**标准解法**：先把两边的原文公式都读出来，找出真正的推导关系再回答；禁止未读原文就猜测性归因（如"翻译问题""原文笔误"）——用户明确说过"不要轻易去质疑我给出的文档！先去想问题为什么会变成这样"。常见真相是：后一处是前一处的特例、推广或具体实现，二者并不矛盾。

**指令前缀**："跨章矛盾先读两边原文，给出推导链，不猜测归因。"

**来源**：chap3 会话 S2（2026-09-06，AI 误答"翻译问题"被用户批评）

---

## [数学推导] 算法对比用分层框架，缺一不可

**触发场景**：解释或比较两种数值方法的区别（如 Gauss-Newton vs Newton、梯度下降 vs 加速梯度、PGM vs FISTA 等）

**标准解法**：多层维度辨析，例如：
1. **近似阶数**（一阶展开还是二阶展开）
2. **操作对象**（作用于方程还是作用于标量代价函数）
3. **求解方式**（解正规方程 vs Hessian 求逆等）

并注意区分"展开式/迭代格式形式相同"与"求解动作不同"——形式相似不等于方法等价。

**指令前缀**："比较两种算法按近似阶数、操作对象、求解方式分层讲，并说明形式相似处与本质差异。"

---

## [符号规范] 全文矩阵形式必须统一

**触发场景**：同一矩阵在章内不同位置采用了不同的分块形式

**标准解法**：在章节开头明确选定一种矩阵分块约定（块对角 / 纵堆叠 / 横堆叠），全文所有公式和脚注必须一致使用。若某处需要切换约定（如 $\mathbf{C}(\bm{\theta})\bm{x} \equiv \tilde{\mathbf{C}}(\bm{x})\bm{\theta}$ 类的等价改写），必须显式说明"此处从约定 A 切换到约定 B，原因如下…"

**指令前缀**："全文使用统一的矩阵分块约定。需要切换时显式声明并给出理由。"

---

## [符号规范] 复共轭 / 转置 / Hermitian 共轭必须严格区分

**触发场景**：涉及复数矩阵的转置操作

**标准解法**：
- 实数矩阵的转置：$\mathbf{A}^T$
- 复数矩阵的 Hermitian 共轭（共轭转置）：$\mathbf{A}^H$
- 不应默认 $\mathbf{A}^T = \mathbf{A}^H$

尤其注意：当文中涉及傅里叶矩阵 $\mathbf{F}$、线圈灵敏度 $\mathbf{C}$（可能为复数）时，转置操作应写 $\mathbf{A}^H$ 而非 $\mathbf{A}^T$。

**指令前缀**："检查所有矩阵转置操作，复数矩阵使用 Hermitian 共轭 H 而非普通转置 T。"

---

## [符号规范] 取实部操作 $\Re$ 不可省略

**触发场景**：原文以"不失一般性"为由省略了 $\Re\{\cdot\}$

**标准解法**：在梯度推导中，如果代价函数是 $\|\cdot\|_2^2$ 形式（实值），则梯度中的 $\Re\{\cdot\}$ 是推导的有机部分，不应省略。若原文省略，应在脚注中补全推导并说明"原文略去 $\Re$ 的理由是 X，完整形式如上"。

**指令前缀**："梯度推导中保留 $\Re\{\cdot\}$。如原文省略，在脚注中补全。"

---

## [符号规范] 用户自身符号习惯优先于文档声明的约定

**触发场景**：AI 依据文档中声明的字体约定（如"矩阵用加粗正体"）批量修改某个符号

**标准解法**：若某符号在全文内使用风格一致（如 $\bm{\Phi}$ 自始至终为加粗斜体），即使与声明约定不符，也**不得**批量替换；应保持原样并在汇报中提示差异，由用户决定。用户可能出于个人习惯或与原文符号保持一致而有意保留。

**指令前缀**："符号风格以用户现有书写为准，声明约定仅供参考；发现偏差只提示、不改写。"

---

## [符号规范] 迭代次数统一用下角标

**触发场景**：算法伪代码、迭代公式、附录符号表中的迭代变量出现 $x^t$、$p^{t+1}$ 等上角标写法

**标准解法**：迭代次数一律用下角标（$\bm{x}_t$、$\bm{x}_{t-1}$、$\bm{p}_{t+1}$）；上角标只留给转置 $\cdot^T$、Hermitian 共轭 $\cdot^H$、幂次（$\|\cdot\|_2^2$、$(\cdot)^k$、$O(1/t^2)$）与方法标签（$\beta^{\mathrm{FR/PR/HS/DY}}$）；极小值点 $\bm{x}^*$、凸共轭 $g^*$ 保持上角标不变。

**指令前缀**："迭代次数一律用下角标；上角标保留给转置/共轭/幂/标签。"

**来源**：chap3 会话 S2（2026-09-06，用户要求全文统一）

---

## [数学写作] `min` 与 `argmin` 不可混用：前者是最优值，后者是最优解

**触发场景**：书写极小化问题的记号（尤其鞍点/极小-极大问题）

**标准解法**：
- $\arg\min$ 返回**取得最优值的点**（解），用于"求重建图像"这类以解为目标的表述；$\min$ 返回**最优值**（数值）。
- **极小-极大问题写 $\min_{\bm{x}}\max_{\bm{v}}L(\bm{x},\bm{v})$ 而不写嵌套的 $\arg\min\arg\max$**：鞍点 $(\bm{x}^*,\bm{v}^*)$ 是由不等式 $L(\bm{x}^*,\bm{v})\le L(\bm{x}^*,\bm{v}^*)\le L(\bm{x},\bm{v}^*)$ 定义的**联合条件**，而嵌套 argmin/argmax 是顺序化的极值点过程，两者一般不等价。
- 文档中单目标问题统一用 argmin、鞍点问题用 min max，属自洽约定，无需"统一"。

**指令前缀**："单目标用 argmin，鞍点用 min max，不强行统一。"

**来源**：chap3 会话 S3（2026-09-10）

---

## [用户偏好] 伪代码关键字用英文，其余内容保持中文

**触发场景**：翻译或撰写伪代码（algorithm / algpseudocode 环境）

**标准解法**：指令关键字（input: / output: / repeat / until / if / then / else / for / do / end / while / loop / break）一律保留英文——这些词译成中文会完全不可读；伪代码的其余内容（`\Require` 参数说明、`\If`/`\Until` 条件文本、`\State` 语句中的自然语言）保持中文。导言区不要把 `\algorithmic*` 系列关键字重命名为中文；标题（\caption）与注释（\Comment）均保持中文。边界示例：`\If{$\|\bm{r}^t\|_2^2 \leq$ 阈值}`——关键字 if/then 英文，条件内容"阈值"中文；`\Until{满足收敛/终止条件}`——关键字 until 英文，条件内容中文。

**指令前缀**："伪代码中指令关键字保持英文，其余内容保持中文；不要批量翻译算法体内的文本。"

**来源**：chap3 会话 S1（2026-09-05）

---

## [用户偏好] 经验与进度记录只写入用户维护的 HANDOFF/LEARNINGS

**触发场景**：AI 需要记录经验教训、用户偏好、会话进度

**标准解法**：一律写入当前章的 `HANDOFF.md`（会话记录、文件状态、待决策项）与 `LEARNINGS.md`（经验错题本），沿用既有格式。禁止另建独立记录文件（如 AI 自带 memory 目录）——这两份文档由用户亲自维护，另起炉灶等于无视其维护成果。用户明确："记忆文档就是指 HANDOFF 和 LEARNINGS 这两个 md 文档"。

**指令前缀**："经验/进度一律写入 HANDOFF 与 LEARNINGS，不另建文件。"

**来源**：chap3 会话 S2（2026-09-06）；S3（2026-09-10 用户重申）

---

## [术语规范] 首次出现带全称括号，索引按首次出现顺序

**触发场景**：正文出现中英对照术语；编写术语索引（附录 B）

**标准解法**：
1. 首次出现格式：中文术语（English Full Name，ABBR），如"梯度下降（Gradient Descent，GD）"；后文只用中文或缩写，不再重复全称
2. 补全缺失全称时照抄既有条目格式；人名公式（FR/PR/HS/DY 等）全称即人名本身，不补
3. 附录 B《术语中英文及缩写索引》：四列（中文术语/英文全称/缩写/页码），按**正文首次出现**先后排列（不含摘要）；首次出现处放隐藏锚点 `\label{term:XX}`，表格中用 `\pageref{term:XX}` 生成页码——自动跟踪、可点击（hyperref），正文改动后页码自动更新
4. 收录范围（沿用既定）：只收括号式缩写术语；无缩写（Line Search、Well-conditioned Matrix、Convex Conjugate、Saddle-point Problem 等）与无括号裸术语暂不收录，扩展与否待用户决策

**指令前缀**："术语首次出现写全称括号（中文（英文全称，缩写）），索引按正文首次出现顺序，页码用 \label+\pageref。"

**来源**：chap3 会话 S1（2026-09-06）；S3（2026-09-10 附录核查）

---

## [文档规范] 附录名录必须与正文同步：排序、锚点、超页三件事

**触发场景**：章节收尾时核查"物理量符号表"（附录 A）与"术语索引"（附录 B）

**标准解法**：
1. **排序**：一律按**正文首次出现顺序**排列。行数多时写一次性 Python 脚本扫描各符号/术语的行号后排序，不靠目测（目测极易错位）。
2. **一致性**：逐条核对 `\label{term:XX}` 与 `\pageref{term:XX}` 一一对应——索引里引用了正文不存在的条目会渲染成 **??**（正文删节而索引未同步是常见成因，须删除该条）；反之正文新增的括号式术语须补录。符号表同理：正文改用的符号（如 Hessian 由 $\nabla^2f$ 改为 $\mathcal{H}f$）、已不再出现的符号都要同步增删。
3. **超页**：行数增加到单表放不下时，按**内容自然边界**拆成多个 `table` 浮动体，保持相同的 tabularx+booktabs+`\small` 格式、表头行重复。不用 \resizebox（字号不可控）、不用 longtable（需改包与浮动体结构）。

**指令前缀**："附录按首次出现排序（脚本核对）、锚点与引用一一对应、超页按内容边界拆分。"

**来源**：chap3 会话 S3（2026-09-10）

---

## [用户偏好] 章节摘要的两段式结构

**触发场景**：为章节撰写/重写摘要

**标准解法**：摘要分两段：
1. **第一段**写章节内容脉络，固定以"本报告基于《Magnetic Resonance Image Reconstruction: Theory, Methods, and Applications》第X章内容，从……出发，通过数学推导梳理了……"开头，随后按"先……随后……之后……最后……"推进。**书名固定为此名，各章一律沿用**。
2. **第二段**先写对原文的勘误补全工作（"本报告对原文的数学推导进行了进一步勘误与补全……并以脚注形式补充了……"），再写个人感悟——**感悟句由用户指定，照录**，AI 不自行发挥。

用户偏好简洁——AI 初稿偏长时用户会自行精简，勿堆砌术语。若章节实际内容已删减，摘要须同步删除对应介绍，不得保留不存在的章节。

**来源**：chap2 S15；chap3 会话 S3 定案完整版（2026-09-10）

---

## [用户偏好] 本用户优先级：数学严谨性 > 工程简洁性

**触发场景**：需要在"严谨完整"和"简洁易懂"之间做取舍

**标准解法**：先给出完整严谨的推导，再以注释或括号方式说明"工程实践中通常简化为…"。

**指令前缀**："该用户为领域初学者，所有技术解释优先保证数学严谨性和逻辑一惯性。工程简化只作附注，不可替代主推导。"

---

## [用户偏好] 不要反问"是否需要"，直接按最高标准执行

**触发场景**：对严谨性要求不确定时，AI 反问用户是否需要补充

**标准解法**：当发现原文省略了某个重要组成部分（如正则化项、取实部、适用条件），应直接补全并按严谨方式推导，最后加注说明原文的处理方式。不要先问用户"要不要加"。

**指令前缀**："遇到省略/简化时默认补全严谨版本，不要反问用户。"

---

## [用户偏好] 审阅深度对标同行评议

**触发场景**：用户要求"检查推导"

**标准解法**：用户说的"检查"不是形式审查，而是：
- 每一步代数变换是否正确
- 每一项的角标是否匹配（Einstein 求和约定下尤其重要）
- 每个等号是否在数学上成立（非工程近似）
- 每个结论的前提条件是否已注明

**指令前缀**："检查推导时按数学证明标准逐行验证，包括角标匹配、前提条件和等号合法性。"

---

## [用户偏好] AI 产出是初稿，用户会亲自改写

**触发场景**：用户要求"重写摘要/写一段话"，AI 完成后用户在自己的编辑器里大幅改写

**标准解法**：把 AI 输出视为供打磨的初稿——用户连"记得加上这句话"的指定句都会微调措辞。改写发生后尊重用户版本，绝不恢复 AI 版本；后续汇报与文档更新一律以用户当前文件状态为准。

**指令前缀**："输出后如用户改写，以用户版本为最终稿，不做恢复。"

**来源**：chap2 S15；chap3 会话 S1、S3（用户多次整段重写 AI 脚注与正文段落）

---

## [用户偏好] "梳理全文"的标准动作

**触发场景**：用户要求"梳理全文"

**标准解法**：三类处理，泾渭分明：
1. 明确错误（错别字、失效引用、漏因子、下标错误、字体约定违反）→ 直接修复；
2. 修复后**逐条汇报**，其中数学内容修正（如漏因子、下标错位）单独加粗提示请用户复核；
3. 存疑内容（原文记号含糊、疑似笔误）→ 只列清单供用户决策，不擅自改动、不反问。

**指令前缀**："明确错误直接修并汇报；存疑内容列清单，不擅自改。"

---

## [用户偏好] 核心说明写正文，脚注只作补充注释

**触发场景**：需要向正文补充说明（概念定义、条件说明、推导）

**标准解法**：属于论证主线的说明（如"CG 此时解的是什么系统、为何正定"）直接写进正文，不要放脚注；脚注只用于旁支补充（术语定义、勘误、细节推导）。用户原话："不必写脚注，直接在正文修改"。

**指令前缀**："主线说明写正文，脚注只作补充。"

**来源**：chap3 会话 S2（2026-09-06）

---

## [用户偏好] 省 token 工作方式

**触发场景**：多轮长对话中的例行操作（记录文档更新、编译、长篇解释）

**标准解法**：1）不每轮更新 HANDOFF/LEARNINGS——长对话结束时由用户手动要求总结，再统一写入；2）不每次修改后编译（必要时才编译）；3）回复抓主要矛盾、不堆砌。

**指令前缀**："省 token：不每轮更新记录文档、不默认编译，长对话结束由用户手动要求总结。"

**来源**：chap3 会话 S2（2026-09-06）

---

## [沟通方式] 用户的"我还是不太懂"是深度追问信号

**触发场景**：用户看到 AI 解释后表示仍然不理解

**标准解法**：这不是用户理解能力的问题，而是之前的解释在某个逻辑环节上出现了跳跃或混淆。应回到更基础的层面，用**函数签名→算子→结果→可逆性**的四步框架重新解释，并检查之前的解释是否混淆了不同数学对象的层次。

**指令前缀**："当用户表示不理解时，回到更基础的数学定义层面重新解释，检查之前的解释是否有逻辑跳跃。"

---

## [沟通方式] "是不是错了"类提问：先论证，不动文档

**触发场景**：用户问"X 是不是错了/是不是多了 Y？如果是就写勘误，如果不是告诉我为什么"，常附"先不用修改文档"

**标准解法**：此类提问默认不改文件。先给明确结论，再给完整论证（与上下文公式的自洽性、与原文的一致性、与相关章节约定的关系），必要时用"去掉它会破坏哪几处"的反证。用户认可结论后才会下"写脚注/勘误"的指令——到那时才动文档。

另有一类同族提问：**"为什么可以这样写"**（追问的是**逻辑衔接**而非对错，如"这两处是不是同一个东西""中间是不是缺了逻辑"）。此类同样先论证不动文档，且论证要给出**前提与动机**：为什么此处可以这样用、中间缺了哪一环、要不要在正文补上这一环。用户认可后会明确指示改法（"直接改正文"或"重写"）。

**指令前缀**："用户问'是不是错了/为什么可以这样写'时只给结论与论证，不修改文档，等确认。"

**来源**：chap3 会话 S1（½ 因子辨析）；S3（§3.2 逻辑衔接）

---

## [沟通方式] 回答问题先抓主要矛盾，一句话结论优先

**触发场景**：用户提问（尤其是"是不是错了/为什么"类），AI 给出长篇解释

**标准解法**：先给一句话结论，再按需展开。禁止东拉西扯堆砌旁支信息（无关行号、无关脚注、次要条件）——用户要的是主要矛盾的直接答案，细节他追问时再给。

**指令前缀**："回答抓主要矛盾，先给一句话结论，不堆砌无关信息。"

**来源**：chap3 会话 S2（2026-09-06）

---

## [沟通方式] 聊天中公式用 Unicode 数学符号

**触发场景**：聊天回复中出现数学公式

**标准解法**：用 Unicode 符号代替 LaTeX 源码（𝒟、ℋ、∇、θ、λ、Σ、ᴴ、⁻¹、ₖ、‖·‖₂、∈、≈、→ 等）；仅完整多行推导等 Unicode 难表达时才写 LaTeX，或写成 .md 文件供 KaTeX 预览（Markdown Preview Enhanced）。注意：用户使用外部 API（`ANTHROPIC_BASE_URL` 指向非 Anthropic 主机时云端同步功能被禁用），不要建议 claude.ai 会话同步方案。

**指令前缀**："聊天公式用 Unicode 数学符号；不要推荐 claude.ai 同步方案。"

---

## [工程实践] algpseudocode 的 \Require/\Ensure 默认不编号

**触发场景**：需要算法伪代码的 input/output 行参与行编号

**标准解法**：algpseudocode 中 `\Require`/`\Ensure` 定义为裸 `\item[...]`，绕过 algorithmicx 的行号机制。要让 input 行作为第 1 行编号，在导言区重定义：`\algrenewcommand{\Require}{\State \algorithmicrequire}`（\Ensure 同理）。后续 `\State` 行号整体 +1。

**指令前缀**："算法 input/output 行参与行编号：\algrenewcommand{\Require}{\State \algorithmicrequire}。"

**来源**：chap3 会话 S2（2026-09-06）

---

## [工程实践] 浮动体内 `\footnote` 会静默丢失：用 `\footnotemark` + `\footnotetext`

**触发场景**：需要在算法（`algorithm`）等浮动体内加脚注

**标准解法**：浮动体内容先被排版进盒子，脚注正文进不了 insert，`\footnote` 的角标可能印出但**正文会无声消失**（不掉错、不报警告，极易漏检）。正确做法：浮动体内写 `\footnotemark`（自增并印角标），紧接 `\end{algorithm}` 之后（浮动体外）写 `\footnotetext{...}`（沿用同一编号）。若浮动体被延迟到下一页，角标与正文可能分页，必要时调整浮动符。

**指令前缀**："浮动体内加脚注：内部 `\footnotemark`、外部 `\footnotetext`。"

**来源**：chap3 会话 S3（2026-09-10）

---

## [工程实践] 新增浮动体会使全文编号顺延，硬编码引用必须改 `\autoref`

**触发场景**：在既有文档中插入新的 `algorithm`/`figure`/`table` 浮动体

**标准解法**：插入前先搜索全文中的**硬编码编号**（如"根据算法5求解"、"见式(3.12)"），一律改为 `\autoref{alg:xx}`。同时顺带排查相近笔误（如"AMG"误写为"AGM"）。插入后编译一遍确认编号与交叉引用正确。

**指令前缀**："插入浮动体后，全文硬编码编号改 `\autoref`。"

**来源**：chap3 会话 S3（2026-09-10）

---

## [工程实践] 本机编译与验证流程

**触发场景**：修改 tex 后需要编译验证

**标准解法**：
- TeX Live 2025 位于 `D:\GanJiacheng\apps\texlive\2025\bin\windows`，文档用 xelatex 编译，**双遍**以解析交叉引用：`xelatex -interaction=nonstopmode -halt-on-error main.tex`
- 正文不使用 `\cite`，故无需 biber；`\printbibliography` 可保留
- 本项目 Git Bash 的 PATH 解析残缺（`grep`/`head`/`rm`/`where`/`cmd` 等不可用），须用**全路径**调用 exe；msys2 工具可用全路径 `D:/GanJiacheng/apps/msys2/usr/bin/rm.exe`；文件搜索/检查用专用工具（Grep/Read）代替 shell 命令
- 编译后检查日志中的 `Float too large`（表格/浮动体超页）、`Overfull`（排版溢出）、失效引用（`??`）
- 完成后清理自建的临时文件

---

## [工程实践] 英文原书 PDF 带权限加密，Read 工具打不开：用 PyMuPDF

**触发场景**：需要读取 `Chapter-*.pdf` 正文（翻译、核对公式）

**标准解法**：Read 工具报 "PDF is password-protected"；本机 Python 3.14 已装 **PyMuPDF（fitz）**、pypdf、python-docx，可直接 `fitz.open(pdf)` 提取文本、导出图片，或用 `doc.save()` 另存**无加密副本**后再用 Read 工具逐页目视核对公式。**纯文本提取的公式会失真，公式必须目视核对**。批量检查（排序、交叉引用核对）也写一次性 Python 脚本执行，用后即删。注意：Python 为 Windows 版，路径不能写 `/tmp`，临时文件放到项目目录内。

**指令前缀**："读英文 PDF 用 PyMuPDF；公式目视核对；脚本用后即删。"

**来源**：chap3 会话 S3（2026-09-10）

---

## [工程实践] 并行编辑碰撞：Edit 失败先 Read 再基于用户版本改编

**触发场景**：AI 执行 Edit 时用户正在并行改写同一段文本（old_string 不匹配）

**标准解法**：立即 Read 目标区域获取用户最新行文；用户改写往往已覆盖部分修改意图，在其版本上只补缺口（保留用户措辞与页码等细节），绝不把 AI 旧版本文字作为目标状态恢复。与本条目同源的既有教训："AI 产出是初稿，用户会亲自改写"——改写后以用户版本为最终稿。

**指令前缀**："编辑失败先 Read，基于用户最新版本改编，不恢复 AI 旧版本。"

**来源**：chap2 S16；chap3 会话 S1、S3
